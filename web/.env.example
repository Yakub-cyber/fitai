# Базовый URL API — нужен ТОЛЬКО если фронт и бэкенд на разных доменах.
# Без слэша на конце. Пример: https://api.fitai.app
# Оставьте пустым, если фронт раздаётся с того же домена, что и API
# (dev-прокси Vite или SERVE_WEB=true на Gateway).
VITE_API_URL=
